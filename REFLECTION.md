What was the hardest part to understand?
I would say the .skipSong method was the most difficult to initally understand because I did not realize it's function of removing songs from the array, rather than cycling through them. 

How does this code use this to tie data and behavior together?
The code uses 'this' to define which object it is using data from, and bases the behaviors off of 'this'. Using 'this' data inside of the object can be changed, or data from inside the object can be used for other functions outside. 

What would you do differently if you wrote this object from scratch?
If I wrote this object from scratch, I would have made the .skipSong method cycle through songs rather then delete them after playing them. Additionally, I would consider another property that specificies whether or not the playlist is playing or not. 
